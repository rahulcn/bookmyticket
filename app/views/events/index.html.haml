.row.index
	- @events.each do |event|
		= render partial: 'event', locals: {event: event}

:javascript
	$('.users').on('click', function() {
		$('.attendee', $(this).parent()).slideToggle();
	});
	$('.close', '.attendee').on('click', function() {
		$(this).parent().slideToggle();
	});
