%nav.navbar.navbar-default{role: "navigation", style: "border-radius: 0;box-shadow: 0px 0px 5px #555;"}
	.navbar-header
		%button.navbar-toggle{"data-target" => "#bs-example-navbar-collapse-1", "data-toggle" => "collapse", type: "button"}
			%span.sr-only Toggle navigation
			%span.icon-bar
			%span.icon-bar
			%span.icon-bar
		%a.navbar-brand{href: "#"} BookMyTicket
	#bs-example-navbar-collapse-1.collapse.navbar-collapse
		%ul.nav.navbar-nav
			%li.active
				%a{href: root_path} Home
			%li
				%a{href: events_path} Events


		%ul.nav.navbar-nav.navbar-right
			- if signed_in?
				%li.active
					= link_to 'Logout', destroy_user_session_path, method: :delete
			- else
				%li.active
					= link_to 'Login', new_user_session_path
				%li
					= link_to 'Register', new_user_registration_path

		-# Dropdown sign_in, sign_up form.
		-#- else
		-#	%ul.nav.navbar-nav.navbar-right
		-#		%li.dropdown
		-#			= render partial: 'shared/sign_up'
		-#		%li.dropdown
		-#			= render partial: 'shared/sign_in'
